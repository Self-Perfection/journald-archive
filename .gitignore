# Build outputs. The deb is built out-of-tree (see packaging/deb/build.sh),
# so debhelper artifacts never land in the source tree — only the final
# package, which build.sh drops into packaging/deb/dist/.
packaging/deb/dist/
*.deb
*.buildinfo
*.changes
