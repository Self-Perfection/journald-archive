# debhelper / dpkg-buildpackage build artifacts
debian/.debhelper/
debian/files
debian/debhelper-build-stamp
debian/journald-archive/
debian/journald-archive.substvars
debian/*.debhelper
*.debhelper.log

# Build outputs (land in the parent dir by convention, but ignore if moved here)
*.deb
*.buildinfo
*.changes
